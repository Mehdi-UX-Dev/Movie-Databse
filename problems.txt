
        
     //! adding the watch providers to the page 

        // adding the every movie and tv specific detail to the app 
        // adding the load more button
     // adding the spinners to the to the main.js
    // adding the google provider 
    // two provider is enough 
    // also adding the the search component in dataFetch so to search again 
    //the prooblem with the serverside authentication in yet to be done 
    // error handling is not completely done like network error in the main page in the build time 
    // adding the what's popular on tv and theatre although the toggle will take time to design 
    // array of pictures for the rendering 
    // rewriting the swr hook 
        // making the names of the components more clearer 

    
https://api.themoviedb.org/3/watch/providers/regions?api_key=<<api_key>>&language=en-US
https://api.themoviedb.org/3/watch/providers/movie?api_key=<<api_key>>&language=en-US
